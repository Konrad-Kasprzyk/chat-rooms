import MAX_HISTORY_RECORDS from "backend/constants/maxHistoryRecords.constant";

const HALF_OF_MAX_RECORDS_BEFORE_SPLIT = Math.floor(MAX_HISTORY_RECORDS / 2);

export default HALF_OF_MAX_RECORDS_BEFORE_SPLIT;
