export default async function createTask(
  workspaceId: string,
  title: string,
  description: string,
  columnId: string
): Promise<string> {
  //   const auth = getAuth();
  // const user = auth.currentUser;
  return "task id";
}
