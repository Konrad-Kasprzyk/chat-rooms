// Works like columns.splice(newIndex, 0, columns[currentIndex]); and removing duplicate
export default async function moveColumnToNewIndex(
  workspaceId: string,
  columnId: string,
  newIndex: number
): Promise<void> {}
