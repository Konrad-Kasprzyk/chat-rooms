import Goal from "common/clientModels/goal.model";
import GoalFilters from "common/types/filters/goalFilters.type";

export default async function getGoals(filters: GoalFilters): Promise<Goal[]> {
  return [];
}
