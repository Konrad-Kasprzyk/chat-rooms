import Norm from "common/models/norm.model";
import NormFilters from "common/types/filters/normFilters.type";

export default async function getNorms(filters: NormFilters): Promise<Norm[]> {
  return null;
}
