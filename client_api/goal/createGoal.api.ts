export default async function createGoal(
  workspaceId: string,
  title: string,
  description: string
): Promise<string> {
  //   const auth = getAuth();
  // const user = auth.currentUser;
  return "goal id";
}
